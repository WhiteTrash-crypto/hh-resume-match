# Server (Netlify env / local .env)
APIFY_TOKEN=
APIFY_ACTOR=
OPENAI_API_KEY=
OPENAI_MODEL=
GOOGLE_SA_JSON=
# Local only (not for Netlify):
GOOGLE_SA_PATH=
SESSION_SECRET=
# Access keys (comma or newline). Each key gets ACCESS_KEY_USES starts (default 2).
ACCESS_KEYS=
ACCESS_KEY_USES=2
# Shown in UI so users can share their Sheet
PUBLIC_GOOGLE_SA_EMAIL=
VITE_GOOGLE_SA_EMAIL=
